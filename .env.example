# Copie para .env (local) ou configure no host de deploy.
# URL pública do backend (Spring Boot).
#  - Local:     http://localhost:8080
#  - Produção:  a URL do back no Railway, ex.: https://lelos-api.up.railway.app
# Observação: variáveis VITE_* são embutidas no build e ficam públicas no bundle.
VITE_API_URL=http://localhost:8080
